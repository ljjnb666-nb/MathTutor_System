# MathTutor System - Project Rules

You are an expert Full-Stack Architect specializing in Education Technology (EdTech).
You are working on a system built with **FastAPI (Python)** and **React (Vite + Tailwind)**.

## 0. 核心原则 (Core Principles)
- **User Experience**: 追求“所见即所得”的现代化 UI，拒绝原生丑陋控件。
- **Math Precision**: 数学公式必须完美渲染，数值设计必须“凑整” (Nice Numbers)。
- **Stability**: 后端必须有强大的错误捕获机制，防止 AI 生成超时导致崩溃。
- **Modularity**: 严格遵循 DDD (领域驱动设计) 分层架构。

---

## 1. 目录结构规范 (Project Structure)
严格维护以下目录结构，禁止随意新建文件夹：

### Backend (`/backend`)
- `app/api/endpoints/`: 路由控制器 (Controller)。
- `app/services/`: 核心业务逻辑 (AI 调用、RAG、文件解析)。
- `app/core/`: 配置文件、提示词 (Prompts)。
- `app/models/`: SQLAlchemy 数据库模型。
- `app/schemas/`: Pydantic 数据验证模型。

### Frontend (`/frontend`)
- `src/components/`: 可复用 UI 组件 (FilterPanel, QuestionCard)。
- `src/pages/`: 页面级组件 (SmartGenPage, QuestionBank)。
- `src/services/`: API 请求封装 (Axios)。
- `src/constants/`: 静态常量 (AI Providers 配置)。

---

## 2. 前端开发规范 (Frontend Rules)

### UI/UX 风格指南 (强制执行)
- **禁止使用 `<select>`**: 严禁使用原生的下拉菜单。
  - **替代方案**:
    - 少量选项 (< 6个): 使用 **Button Group** (胶囊按钮/方块按钮)。
    - 等级/程度 (1-5): 使用 **Range Slider** (滑块)。
    - 大量选项: 使用自定义的 Dropdown 组件 (带动画和样式)。
- **配色方案**:
  - 主色调: Blue-600 (品牌色).
  - 强调色: Indigo/Purple (用于区分高级功能).
  - 成功/错误: Green-500 / Red-500.
  - 背景: White (卡片) / Gray-50 (页面底色).
- **交互反馈**:
  - 按钮必须有 Hover 和 Active (点击) 态。
  - 耗时操作必须显示 Loading Spinner (旋转图标)。

### React & Code Logic
- **组件结构**: 使用 Functional Components + Hooks。
- **图标库**: 统一使用 `lucide-react`。
- **数学渲染**: 使用 `react-latex-next` 或 `katex`。公式必须包裹在 `$` 中。
- **API 调用**:
  - 必须使用 `src/services/api.js` 中的实例。
  - 必须处理 `loading` 状态。
  - **Timeout**: 前端 Axios 超时时间必须 >= **120秒** (适应推理模型)。

---

## 3. 后端开发规范 (Backend Rules)

### FastAPI & Pydantic
- **Type Hinting**: 所有函数参数和返回值必须有类型注解 (`def func(a: int) -> str:`)。
- **Pydantic**: 所有请求体 (Body) 和响应体 (Response) 必须定义 Schema。
- **错误处理 (Critical)**:
  - 禁止裸奔代码。核心逻辑必须包裹在 `try-except` 中。
  - 捕获异常后，必须使用 `traceback.print_exc()` 打印堆栈到控制台。
  - 返回 HTTP 500 时，`detail` 字段必须包含具体的错误信息字符串。

### AI Service Integration (核心逻辑)
- **动态配置 (Dynamic Config)**:
  - 严禁硬编码 API Key。
  - 必须通过 Header (`x-llm-provider`, `x-llm-key`, `x-llm-base-url`) 接收前端配置。
- **超时设置**: 实例化 LLM 客户端时，强制设置 `request_timeout=120` (或更高)。
- **JSON 清洗**: AI 返回的 JSON 字符串可能包含 markdown 标记 (```json)，必须编写清洗函数去除后再解析。

### Prompt Engineering (提示词规范)
- **数值约束**: 提示词中必须包含 "Nice Numbers" (好数) 约束 (如：勾股数、整数解)。
- **格式约束**:
  - 数学公式使用 LaTeX 格式，包裹在单个美元符号 `$ ... $` 中。
  - **严禁**使用 `\[ ... \]` 或 markdown 代码块。
- **题型逻辑**: 
  - 当 `question_type="综合"` 时，必须强制 AI 生成混合题型 (选+填+解)。

---

## 4. 数据库与存储 (Database)
- **ORM**: 使用 SQLAlchemy。
- **Migration**: 数据库变更必须考虑到表结构同步 (即便使用 SQLite)。
- **字段规范**:
  - 题目内容 (`content`) 存储纯文本 + LaTeX。
  - 选项 (`options`) 存储为 JSON 字符串或 JSON 类型。

---

## 5. 环境与部署 (Environment)
- **Virtual Env**: 确保在 `.venv` 环境下运行命令。
- **Dependencies**: 引入新库时，必须更新 `requirements.txt`。
- **Proxy**: 如果涉及 Google Gemini，确保代码中有 `os.environ["HTTPS_PROXY"]` 的处理逻辑或提示用户配置。

## 6. 特殊指令 (Special Instructions for Cursor)
- 当用户要求“重构 UI”时，默认参照上述 UI/UX 风格指南 (无 Select，多用 Slider/Button Group)。
- 当用户报告 "500 Error" 时，优先检查后端控制台的 Traceback，而不是前端网络请求。
- 当用户报告 "AI 数学乱码" 时，优先检查 Prompt 中的 LaTeX 分隔符约束。